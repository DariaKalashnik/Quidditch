<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/game_rules_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/poup_winner">

    <TextView
        android:id="@+id/game_rules_title"
        style="@style/TextRules"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <ScrollView
        style="@style/ScrollText"
        android:layout_below="@id/game_rules_title">

        <RelativeLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/game_rules_description"
                style="@style/TextText"
                android:layout_margin="@dimen/value_10"
                android:text="@string/rulesintro" />

            <TextView
                style="@style/TextText"
                android:layout_below="@id/game_rules_description"
                android:text="@string/rulesequp" />

        </RelativeLayout>
    </ScrollView>
</RelativeLayout>